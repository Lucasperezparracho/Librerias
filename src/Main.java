public class Main {
    public static void main(String[] args) {

        boolean resultado = false;


        resultado = EntradaSalida.salida("Hello", EntradaSalida.SALIDA_CONSOLA);
        resultado = EntradaSalida.salida("Hello", EntradaSalida.SALIDA_WINDOW);

    }
}